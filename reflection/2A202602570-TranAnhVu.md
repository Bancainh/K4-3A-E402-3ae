### Trần Anh Vũ — 2A202602570

- **Vai trò/phần việc được nhận:** Prototype Engineer / User flow, prototype, AI integration, demo

- **Những gì tôi đã thay đổi trong repo chung:**
Dựng prototype luồng chat CP2 (6 màn hình + thanh nav demo) và sơ đồ luồng tĩnh bằng HTML/CSS/JS thuần; tích hợp Gemini API thật vào index.html (system prompt ràng buộc theo transcript, schema JSON 4 trạng thái answered/no_evidence/clarify/out_of_scope, xử lý lỗi timeout/rate limit/key sai); nâng cấp từ 1 transcript hardcode sang retrieval tự động trên toàn bộ 6 file transcript; sửa 2 lỗi logic (phân loại clarify sai, thuật toán retrieval bị từ phổ biến lấn át) và 1 vấn đề usability (citation không bấm được) phát hiện qua eval và qua kiểm thử người dùng CP5.

- **File hoặc artifact liên quan:**
index.html (prototype chính CP3–CP5), codebase/mock.html (mockup CP2), codebase/flow-diagram.html (sơ đồ luồng), scripts/build-transcripts.js (build script tách transcript thành JSON để retrieval), .env/.env.example (cấu hình API key).

- **Một quyết định kỹ thuật tôi đã đưa ra và lý do:**
Chọn retrieval bằng chấm điểm từ khoá/n-gram chạy thẳng trong trình duyệt (không dùng embedding/vector search) cho retrieveTopK(), vì prototype là 1 file HTML tĩnh không có backend — ưu tiên tốc độ triển khai và dễ debug (in được điểm số ra console để soi) hơn là độ chính xác semantic cao, chấp nhận đánh đổi này cho quy mô ~700 đoạn của hackathon.

- **Khó khăn tôi gặp và cách tôi xử lý:**
Sau khi chạy eval/golden_set.csv (20 case, 6 case thuộc ambiguous/weak_context), results_v1.csv cho thấy cả 6/6 case này FAIL — hệ thống không bao giờ chọn "clarify", model nhầm "có nội dung liên quan để tổng hợp" với "câu hỏi đủ rõ". Tôi viết lại tiêu chí phân biệt answered/clarify/no_evidence cụ thể hơn kèm few-shot trong system prompt (commit acab7a9, tinh chỉnh thêm ở a4522f1) mà không đổi schema/code, và results_v2.csv xác nhận cả 6/6 case đã PASS. Một lỗi khác tôi tự phát hiện khi test thủ công trước khi commit: câu hỏi chứa từ chuyên ngành hiếm ("parameter") bị loại khỏi top-8 vì các đoạn khác hoà điểm nhờ cụm từ phổ biến ("mô hình AI") — xử lý bằng cách chỉ cộng điểm cụm từ khi cụm đó chứa ít nhất 1 từ dài/hiếm (hasLongWord()).

- **Điều tôi học được từ phần việc này:**
Một system prompt đọc "có vẻ đủ rõ" vẫn có thể sai hoàn toàn ở 1 nhánh logic (0/6 case clarify pass) nếu không chạy qua bộ case thật có chủ đích — lỗi này không lộ ra khi tự đọc lại prompt mà chỉ lộ khi có eval định lượng. Tương tự, một heuristic chấm điểm tưởng đơn giản rất dễ bị các từ phổ biến trong domain hẹp (khoá học AI thì "AI", "mô hình" xuất hiện khắp nơi) lấn át tín hiệu thật sự đặc trưng, nên luôn cần test bằng câu hỏi chứa thuật ngữ hiếm, không chỉ câu hỏi chung chung.

- **Nếu làm lại, tôi sẽ cải thiện điều gì:**
Việc tích hợp API thật xong lúc 23:45 16/9 nhưng phải đến 09:30 17/9 lỗi phân loại clarify mới được phát hiện — nhờ golden set của người khác chạy sau đó, chứ không phải do tôi tự test từng nhánh ngay khi vừa nối API; lần sau sẽ tự viết ngay vài câu hỏi test cho đủ 4 nhánh trạng thái tại thời điểm tích hợp, thay vì đợi eval riêng mới lộ ra. Việc citation không bấm được cũng lẽ ra nên làm ngay từ CP2/CP3 vì "khả năng kiểm chứng câu trả lời" chính là job-to-be-done gốc trong spec, thay vì để đến khi P2 phản hồi Severity High ở CP5 mới sửa (commit dae7d06).
